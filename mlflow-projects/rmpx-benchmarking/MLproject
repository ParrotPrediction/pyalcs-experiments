name: RMPX Benchmarking

entry_points:
  main:
    parameters:
      rmpx-size: { type: int }
      trials: { type: int }
      bins: { type: int }
      agent: { type: string }
    command: "python code/train.py
                        --rmpx-size {rmpx-size}
                        --trials {trials}
                        --bins {bins}
                        --agent {agent}"
